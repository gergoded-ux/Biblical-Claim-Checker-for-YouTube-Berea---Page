import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/Biblical-Claim-Checker-for-YouTube-Berea---Page",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
